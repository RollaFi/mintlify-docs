---
title: 'Direct Payment API'
api: 'POST https://api.rollapay.com/transaction/initiate'
description: 'Process payments directly from your server via card, bank transfer, or mobile money'
---

## Overview

The Direct Payment API lets you process payments directly from your server. Depending on the channel, you can charge a card, initiate a bank transfer (virtual account), or accept mobile money.

**Use this when:**
- You have your own payment form and collect card details yourself
- You want to generate a virtual account for bank transfer payments
- You need full control over the checkout UI
- You're building a mobile app or custom integration

<Note>
  If you don't want to handle card details, use the [Checkout API](/api-reference/checkout-api) instead — it provides a secure hosted payment page.
</Note>

## Payment Methods

Choose the payment method that fits your use case. Each page includes full request/response examples, parameters, and integration steps.

<CardGroup cols={3}>
  <Card title="Card Payment" icon="credit-card" href="/api-reference/card-payment">
    Charge a customer's card directly. Supports 3D Secure, OTP, and PIN authentication flows.
  </Card>
  <Card title="Bank Transfer" icon="building-columns" href="/api-reference/bank-transfer-payment">
    Generate a temporary virtual account for the customer to transfer to. Available for NGN payments.
  </Card>
  <Card title="Mobile Money" icon="mobile" href="/api-reference/mobile-money-payment">
    Accept mobile money payments via STK push. Supports GHS, KES, and ZMW currencies.
  </Card>
</CardGroup>

## Authentication

All requests must include your secret API key in the Authorization header:

```
Authorization: Bearer sk_live_your_secret_key
```

<Note>
  Use `sk_test_*` keys for testing and `sk_live_*` keys for production.
</Note>

## Common Request Parameters

These parameters are shared across all payment channels.

<ParamField body="reference" type="string">
  Optional custom transaction reference. If not provided, a unique reference will be auto-generated (e.g., `TXN_gGbEEvK25eVgmgrf`). Must be globally unique across all transactions.
</ParamField>

<ParamField body="amount" type="number" required>
  The payment amount (e.g., 1000 for 1000 units of the currency)
</ParamField>

<ParamField body="currency" type="string" required>
  Three-letter ISO currency code (e.g., "USD", "NGN", "GBP")
</ParamField>

<ParamField body="channel" type="string" required>
  Payment channel: `card`, `bank_transfer`, `mobile_money`, `palmpay`, `owlpay`, or `ussd`
</ParamField>

<ParamField body="customer" type="object" required>
  Customer information
  <Expandable title="properties">
    <ParamField body="email" type="string" required>
      Customer's email address
    </ParamField>
    <ParamField body="name" type="string">
      Customer's full name
    </ParamField>
    <ParamField body="phone" type="string">
      Customer's phone number. Contact detail only — for mobile money, the wallet to debit goes in `mobileMoney.phone`.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="card" type="object">
  **Required when `channel` is `card`.** See [Card Payment](/api-reference/card-payment) for full details.
  <Expandable title="properties">
    <ParamField body="number" type="string" required>
      Card number, 13–19 digits
    </ParamField>
    <ParamField body="expiryMonth" type="string" required>
      Two digits, `01`–`12`
    </ParamField>
    <ParamField body="expiryYear" type="string" required>
      Two or four digits
    </ParamField>
    <ParamField body="cvv" type="string" required>
      3 or 4 digits
    </ParamField>
    <ParamField body="holderName" type="string">
      Cardholder name (max 100 characters)
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="mobileMoney" type="object">
  **Required when `channel` is `mobile_money`.** See [Mobile Money Payment](/api-reference/mobile-money-payment) for full details.
  <Expandable title="properties">
    <ParamField body="phone" type="string" required>
      The mobile money wallet to debit, including country code (max 20 characters)
    </ParamField>
    <ParamField body="provider" type="string">
      Wallet provider, e.g. `MTN`, `VODAFONE`, `AIRTELTIGO` (max 50 characters)
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="bankTransfer" type="object">
  Optional for `bank_transfer`. Virtual-account providers do not need a bank code upfront.
  <Expandable title="properties">
    <ParamField body="bankCode" type="string" required>
      Bank code
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="callbackUrl" type="string" required>
  URL to redirect the customer after payment completion. Query parameters `reference` and `status` (successful/failed) will be appended.
</ParamField>

<ParamField body="description" type="string">
  Optional description for the payment (max 500 characters)
</ParamField>

<ParamField body="metadata" type="object">
  Additional key-value pairs to attach to the transaction (e.g., order ID)
</ParamField>

## Common Response Fields

<ResponseField name="success" type="boolean">
  Indicates if the request was successful
</ResponseField>

<ResponseField name="message" type="string">
  Response message
</ResponseField>

<ResponseField name="data" type="object">
  <Expandable title="properties">
    <ResponseField name="reference" type="string">
      Unique transaction reference (e.g., `TXN_gGbEEvK25eVgmgrf`) or your custom reference if provided
    </ResponseField>
    <ResponseField name="status" type="string">
      Transaction status (see table below)
    </ResponseField>
  </Expandable>
</ResponseField>

## Transaction Statuses

| Status | Description |
|--------|-------------|
| `pending` | Transaction has been created but not yet processed |
| `requires_action` | Additional action required (3DS, PIN, OTP) |
| `processing` | Transaction is being processed |
| `successful` | Transaction completed successfully |
| `failed` | Transaction failed |
